use crate::{installation_token_data::{read_file, InstallationToken}, webhook_data::WebWebHook};
use anyhow::Result;

pub async fn increase_version(webhook: WebWebHook) -> Result<()> {
    //let mut res = read_file().await?;
    Ok(())
}
